/** @type {import('next').NextConfig} */
const nextConfig = {
  // Render Web Service: Next uses PORT from env automatically
  // For Static Site: uncomment below and set Publish Directory = out
  // output: 'export',
};

export default nextConfig;
